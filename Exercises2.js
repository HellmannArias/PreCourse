const user = {
    name: 'Erik',
    description: 'Me gusta la programación'
};

const user2 = {
    name: 'Daniel',
    description: 'Estoy aprendiendo a programar'
};

console.log(user.description.slice(0, 2).length);
console.log(user2.description.slice(0, 5).length);